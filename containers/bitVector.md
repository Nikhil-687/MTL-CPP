# bitVector.cpp

increasing powers of general vector.cpp

concept is to allocate memory in bit wise fasion without promotions to make it space optimized.

to do so we will need to keep a track of one extra field at the object i.e. currBit